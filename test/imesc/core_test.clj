(ns imesc.core-test
  (:require [clojure.test :refer :all]
            [imesc.core :refer :all]
            [environ.core :refer [env]]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
