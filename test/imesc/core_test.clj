(ns imesc.core-test
  (:require [imesc.core :as sut]
            [clojure.test :refer :all]))

(deftest transformation
  (testing "must be able to transform request into alarm specification"
    (is true true)))
